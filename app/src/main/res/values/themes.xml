<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ACM" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <!--Base.Theme.ACM-->
    <style name="Theme.ACM" parent="Theme.Design.NoActionBar" />

    <style name="RaisedButton" parent="Widget.AppCompat.Button.Colored">
        <item name="backgroundTint">@color/primaryAccent</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="elevation">@dimen/_32dp</item>
    </style>

    <style name="FlatButtonOffSet" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/linkButton</item>
    </style>
      
    <style name="popupMenuHiden" parent="ThemeOverlay.AppCompat.Light">
        <item name="background">@android:color/white</item>
    </style>
    
    <style name="AlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">#0000FF</item>
    </style>

</resources>